## 快速启动指南

### 项目已成功创建！

你的 Vue3 + Cesium.js 项目已准备就绪。

### 启动开发服务器

```bash
npm run dev
```

然后打开浏览器访问 `http://localhost:5173/`

### 加载 3D Tiles 模型

#### 步骤 1：准备模型文件

你的 3D Tiles 模型应该包含以下结构：
```
public/models/
├── tileset.json          # 主配置文件
├── 0/                    # 瓦片层级目录
│   ├── 0/
│   │   └── 0.b3dm       # 3D 模型瓦片
│   └── metadata.json
└── metadata.json
```

#### 步骤 2：配置模型路径

编辑 `src/components/CesiumViewer.vue` 文件，找到 `loadTileset` 的调用部分（大约在第 44 行）：

```typescript
await loadTileset('/models/tileset.json')
```

将路径修改为你的模型路径。

### 主要特性

✅ 禁用地球显示 - 只显示 3D 模型  
✅ 黑色背景 - 增强模型视觉效果  
✅ 自动视图适配 - 自动缩放到模型合适的大小  
✅ 全屏支持 - 点击右上角全屏按钮  
✅ 简洁的 UI - 隐藏地图控件和不必要的按钮  

### 自定义配置

编辑 `src/components/CesiumViewer.vue`，你可以配置：

```typescript
// 背景颜色
viewer.value.scene.backgroundColor = Cesium.Color.fromCssColorString('#000000')

// 显示/隐藏特定的 UI 控件
// 在 new Cesium.Viewer() 的配置对象中修改
```

### 常用命令

```bash
npm run dev      # 启动开发服务器
npm run build    # 构建生产版本
npm run preview  # 预览生产构建
```

### 文件说明

- **src/components/CesiumViewer.vue** - 核心 Cesium 查看器组件
- **src/App.vue** - 主应用程序
- **public/models/** - 放置 3D Tiles 模型的目录
- **vite.config.ts** - Vite 构建配置

### 需要帮助？

- 查看 `README.md` 了解更多信息
- 查看 `public/models/README.md` 了解模型放置要求
- 访问 [Cesium 官方文档](https://cesium.com/docs/) 了解更多 API

祝你使用愉快！🎉
